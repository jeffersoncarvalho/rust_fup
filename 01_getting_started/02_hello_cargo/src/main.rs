/**
 * Testando a versão do Cargo
 * 
 * cargo --version
 * 
 * Criando um novo projeto, com nome personalizado e sem versão de controle
 * 
 * cargo new <nome_do_projeto> --name <nome_do_pacote> --vcs none 
 * 
 * Build
 * $ cargo build
 * Executando
 * $ cargo run
 * Checando
 * $ cargo check
 * Release
 * $ cargo build --release
 * 
 */
fn main() {
    println!("Hello, world!");
}
